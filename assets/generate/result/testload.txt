<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Testload extends CI_Controller {
    function __construct() {
        parent::__construct();
    }

    public $table_db = "coder";
    public $controller = "testload";
    
    public function index() {
        $lbl_controller = str_replace("_", " ", $this->controller);
        $page_url = site_url($this->controller);
		$data = array(
            'controller' => $this->controller,
            'lbl_controller' => $lbl_controller,
            'page_url' => $page_url,
            'rs_coder' => getByQuery('select * from coder'),
                'rs_w' => getByQuery('select * from w'),
                
        );
        $this->layout->display('page/testload_vw', $data);
    }

    public function gridview() {
        $where = $this->input->post("where");        
        $database = "default";
        $column = array('row_id', 'd', 'xx');
        $table = "vw_coderlist";
        $list = $this->datatables_mdl->get_datatables($database, $table, $column, $where);
        $data = array();
        $no = $_POST['start'];
        foreach ($list as $line) 
        {
            $no++;          
            $row = array();
            $link = '
                <div class="btn-group">
                  <a href="javascript:void(0)" data-toggle="dropdown" style="color:#111"><i class="fa fa-folder-open"></i></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="javascript:void(0)" style="color:#111" data-toggle="modal" data-target="#modalForm" onclick="generateModalView(\''.$line->row_id.'\')">View</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="generateModalForm(\'edit\', \''.$line->row_id.'\')">Update</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="doDelete(\''.$line->row_id.'\')">Delete</a></li>
                  </ul>
                </div>
            ';
            $row[] = '<div style="text-align:center">'.$link.'</div>';
            $row[] = '<div style="text-align:center">'.$line->coder_sss.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->w_w.'</div>'; 

            $data[] = $row;
        }
 
        $output = array(
            "draw" => $_POST['draw'],
            "recordsTotal" => $this->datatables_mdl->count_all($database, $table, $column),
            "recordsFiltered" => $this->datatables_mdl->count_filtered($database, $table, $column, $where),
            "data" => $data,
        );
        
        echo json_encode($output);
    }

    public function form() {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtd = ''; 
$coder_list = "";$txtcodersss = ''; 
$txtxx = ''; 
$w_list = "";$txtww = ''; 


        if($txtstate == "edit") {
            $qry = "select * from vw_coderlist where row_id = '".$txtrowid."'";
            $rs = getByQuery($qry);
            if($rs != "") {
                $txtd = $rs[0]->d; 
$txtcodersss = $rs[0]->coder_sss; 
$txtxx = $rs[0]->xx; 
$txtww = $rs[0]->w_w; 

            }
        }

        
                  $rs_coder = getByQuery("select * from coder");
                  if($rs_coder != "") {
                      foreach($rs_coder as $row_coder) {
                          $selected = ($row_coder->ss == $txtd) ? "selected" : "";
                          $coder_list .= "<option value='".$row_coder->ss."' ".$selected." >".$row_coder->sss."</option>";
                      }
                  }
                
                  $rs_w = getByQuery("select * from w");
                  if($rs_w != "") {
                      foreach($rs_w as $row_w) {
                          $selected = ($row_w->w == $txtxx) ? "selected" : "";
                          $w_list .= "<option value='".$row_w->w."' ".$selected." >".$row_w->w."</option>";
                      }
                  }
                

        $table = '
            <div class="row">
                <form method="POST" class="formInput" enctype="multipart/form-data">
                    <input type="hidden" id="txtrowid" name="txtrowid" class="form-control" value="'.$txtrowid.'" style="background:#fff" readonly />
                    <input type="hidden" id="txtstate" name="txtstate" class="form-control" value="'.$txtstate.'" />
                    
                  <div class="col-md-12">
                    <div class="form-group">
                      <label>Ddd:</label>
                      <select id="txtd" name="txtd" class="form-control select2"  required>
                        <option value=""></option>
                        '.$coder_list.'
                      </select>
                    </div>
                  </div>
                
                  <div class="col-md-12">
                    <div class="form-group">
                      <label>Ww:</label>
                      <select id="txtxx" name="txtxx" class="form-control select2"  required>
                        <option value=""></option>
                        '.$w_list.'
                      </select>
                    </div>
                  </div>
                
                </form>
            </div>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function view() {
        $txtrowid = $this->input->post("txtrowid");
        $txtd = ''; 
$coder_list = "";$txtcodersss = ''; 
$txtxx = ''; 
$w_list = "";$txtww = ''; 

        
        $qry = "select * from vw_coderlist where row_id = '".$txtrowid."'";
        $rs = getByQuery($qry);
        if($rs != "") {
                $txtd = $rs[0]->d; 
$txtcodersss = $rs[0]->coder_sss; 
$txtxx = $rs[0]->xx; 
$txtww = $rs[0]->w_w; 

        }        

        $table = '
            <table id="tablelist" class="table table-striped">
              <tbody>
                
                  <tr>
                      <td class="view-title" style="width: 30%">Ddd:</td>
                      <td class="view-txt">'.$txtcodersss.'</td>
                  </tr>
                
                  <tr>
                      <td class="view-title" style="width: 30%">Ww:</td>
                      <td class="view-txt">'.$txtww.'</td>
                  </tr>
                
              </tbody>
            </table>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function save()
    {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtd = $this->input->post("txtd"); $txtxx = $this->input->post("txtxx"); $txtxx_hdn = $this->input->post("txtxx_hdn"); 
        
                $txtxx =  $txtxx_hdn;
                if($_FILES['txtxx']['name'] != '')
                {
                     $ext = pathinfo($_FILES['txtxx']['name'], PATHINFO_EXTENSION);
                     $file_name = date('YmdHis').'.'.$ext;
                     $config = array(
                        'upload_path' => 'uploaded',
                        'allowed_types' => 'gif|jpg|png|jpeg|pdf|docx|xls|xlsx|doc|txt',
                        'overwrite' => TRUE,
                        'file_name' =>  $file_name
                    );
                     $this->upload->initialize( $config);
                    if( $this->upload->do_upload('txtxx'))
                    {
                         $data = $this->upload->data();
                        $txtxx =  $file_name;
                        if($txtxx_hdn != '')
                          unlink('uploaded/'.$txtxx_hdn);
                    }
                    else
                    {
                        $data['msg'] = $this->upload->display_errors();
                        $data['type'] = 0;
                        echo json_encode($data);
                        exit();
                    }
                }
              

        if($txtstate == 'add')
        {
            $insert = array(
                'd' => $txtd, 'xx' => $txtxx,         
            );
            insertData($this->table_db, $insert);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
        else
        {            
            $update = array(
                'd' => $txtd, 'xx' => $txtxx,              
            );
            updateData($this->table_db, $update, "row_id = '".$txtrowid."'", $txtrowid);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
    }

    public function delete(){
        $txtrowid = $this->input->post("txtrowid");
        
        deleteData($this->table_db, "row_id = '".$txtrowid."'");
        $data['msg'] = "Process successful";
        $data['type'] = 1;
        echo json_encode($data);
    }
}	