<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Curso_disciplina extends CI_Controller {
    function __construct() {
        parent::__construct();
        islogin();
    }

    public $table_db = "educ_curso_disciplina";
    public $controller = "curso_disciplina";
    
    public function index() {
        $lbl_controller = str_replace("_", " ", $this->controller);
        $page_url = site_url($this->controller);
		$data = array(
            'controller' => $this->controller,
            'lbl_controller' => $lbl_controller,
            'page_url' => $page_url,
            'rs_educ_disicplina' => getByQuery('select * from educ_disicplina'),
                'rs_educ_operador' => getByQuery('select * from educ_operador'),
                
        );
        $this->layout->display('page/curso_disciplina_vw', $data);
    }

    public function gridview() {
        $where = $this->input->post("where");        
        $database = "default";
        $column = array('row_id', 'curso_id', 'diciplina_id', 'operador_id', 'data_acesso');
        $table = "vw_educ_curso_disciplinalist";
        $list = $this->datatables_mdl->get_datatables($database, $table, $column, $where);
        $data = array();
        $no = $_POST['start'];
        foreach ($list as $line) 
        {
            $no++;          
            $row = array();
            $link = '
                <div class="btn-group">
                  <a href="javascript:void(0)" data-toggle="dropdown" style="color:#111"><i class="fa fa-folder-open"></i></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="javascript:void(0)" style="color:#111" data-toggle="modal" data-target="#modalForm" onclick="generateModalView(\''.$line->row_id.'\')">View</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="generateModalForm(\'edit\', \''.$line->row_id.'\')">Update</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="doDelete(\''.$line->row_id.'\')">Delete</a></li>
                  </ul>
                </div>
            ';
            $row[] = '<div style="text-align:center">'.$link.'</div>';
            $row[] = '<div style="text-align:center">'.$line->curso_id.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->educ_disicplina_{diciplina_id}-{nome}.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->educ_operador_{operador_id}-{nome}.'</div>'; 
$row[] = '<div style="text-align:center">'.date("m/d/Y", strtotime($line->data_acesso)).'</div>'; 

            $data[] = $row;
        }
 
        $output = array(
            "draw" => $_POST['draw'],
            "recordsTotal" => $this->datatables_mdl->count_all($database, $table, $column),
            "recordsFiltered" => $this->datatables_mdl->count_filtered($database, $table, $column, $where),
            "data" => $data,
        );
        
        echo json_encode($output);
    }

    public function form() {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtcursoid = ''; 
$txtdiciplinaid = ''; 
$educ_disicplina_list = "";$txteducdisicplina{diciplinaid}-{nome} = ''; 
$txtoperadorid = ''; 
$educ_operador_list = "";$txteducoperador{operadorid}-{nome} = ''; 
$txtdataacesso = ''; 


        if($txtstate == "edit") {
            $qry = "select * from vw_educ_curso_disciplinalist where row_id = '".$txtrowid."'";
            $rs = getByQuery($qry);
            if($rs != "") {
                $txtcursoid = $rs[0]->curso_id; 
$txtdiciplinaid = $rs[0]->diciplina_id; 
$txteducdisicplina{diciplinaid}-{nome} = $rs[0]->educ_disicplina_{diciplina_id}-{nome}; 
$txtoperadorid = $rs[0]->operador_id; 
$txteducoperador{operadorid}-{nome} = $rs[0]->educ_operador_{operador_id}-{nome}; 
$txtdataacesso = date('Y-m-d', strtotime($rs[0]->data_acesso)); 

            }
        }

        
                  $rs_educ_disicplina = getByQuery("select * from educ_disicplina");
                  if($rs_educ_disicplina != "") {
                      foreach($rs_educ_disicplina as $row_educ_disicplina) {
                          $selected = ($row_educ_disicplina->diciplina_id == $txtdiciplinaid) ? "selected" : "";
                          $educ_disicplina_list .= "<option value='".$row_educ_disicplina->diciplina_id."' ".$selected." >".$row_educ_disicplina->{diciplina_id}-{nome}."</option>";
                      }
                  }
                
                  $rs_educ_operador = getByQuery("select * from educ_operador");
                  if($rs_educ_operador != "") {
                      foreach($rs_educ_operador as $row_educ_operador) {
                          $selected = ($row_educ_operador->operador_id == $txtoperadorid) ? "selected" : "";
                          $educ_operador_list .= "<option value='".$row_educ_operador->operador_id."' ".$selected." >".$row_educ_operador->{operador_id}-{nome}."</option>";
                      }
                  }
                

        $table = '
            <div class="row">
                <form method="POST" class="formInput" enctype="multipart/form-data">
                    <input type="hidden" id="txtrowid" name="txtrowid" class="form-control" value="'.$txtrowid.'" style="background:#fff" readonly />
                    <input type="hidden" id="txtstate" name="txtstate" class="form-control" value="'.$txtstate.'" />
                    
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Curso:</label>
                        <input type="number" step="1" " id="txtcursoid" name="txtcursoid" class="form-control" value="'.$txtcursoid.'" />
                      </div>
                    </div>
                  
                  <div class="col-md-12">
                    <div class="form-group">
                      <label>Disciplina:</label>
                      <select id="txtdiciplinaid" name="txtdiciplinaid" class="form-control select2"  required>
                        <option value=""></option>
                        '.$educ_disicplina_list.'
                      </select>
                    </div>
                  </div>
                
                  <div class="col-md-12">
                    <div class="form-group">
                      <label>Operador:</label>
                      <select id="txtoperadorid" name="txtoperadorid" class="form-control select2"  required>
                        <option value=""></option>
                        '.$educ_operador_list.'
                      </select>
                    </div>
                  </div>
                
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Acesso:</label>
                        <input type="date" id="txtdataacesso" name="txtdataacesso" class="form-control" value="'.$txtdataacesso.'" />
                      </div>
                    </div>
                  
                </form>
            </div>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function view() {
        $txtrowid = $this->input->post("txtrowid");
        $txtcursoid = ''; 
$txtdiciplinaid = ''; 
$educ_disicplina_list = "";$txteducdisicplina{diciplinaid}-{nome} = ''; 
$txtoperadorid = ''; 
$educ_operador_list = "";$txteducoperador{operadorid}-{nome} = ''; 
$txtdataacesso = ''; 

        
        $qry = "select * from vw_educ_curso_disciplinalist where row_id = '".$txtrowid."'";
        $rs = getByQuery($qry);
        if($rs != "") {
                $txtcursoid = $rs[0]->curso_id; 
$txtdiciplinaid = $rs[0]->diciplina_id; 
$txteducdisicplina{diciplinaid}-{nome} = $rs[0]->educ_disicplina_{diciplina_id}-{nome}; 
$txtoperadorid = $rs[0]->operador_id; 
$txteducoperador{operadorid}-{nome} = $rs[0]->educ_operador_{operador_id}-{nome}; 
$txtdataacesso = date('Y-m-d', strtotime($rs[0]->data_acesso)); 

        }        

        $table = '
            <table id="tablelist" class="table table-striped">
              <tbody>
                
                    <tr>
                        <td class="view-title" style="width: 30%">Curso:</td>
                        <td class="view-txt">'.$txtcursoid.'</td>
                    </tr>
                  
                  <tr>
                      <td class="view-title" style="width: 30%">Disciplina:</td>
                      <td class="view-txt">'.$txteducdisicplina{diciplinaid}-{nome}.'</td>
                  </tr>
                
                  <tr>
                      <td class="view-title" style="width: 30%">Operador:</td>
                      <td class="view-txt">'.$txteducoperador{operadorid}-{nome}.'</td>
                  </tr>
                
                    <tr>
                        <td class="view-title" style="width: 30%">Acesso:</td>
                        <td class="view-txt">'.$txtdataacesso.'</td>
                    </tr>
                  
              </tbody>
            </table>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function save()
    {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtcursoid = $this->input->post("txtcursoid"); $txtdiciplinaid = $this->input->post("txtdiciplinaid"); $txtoperadorid = $this->input->post("txtoperadorid"); $txtdataacesso = $this->input->post("txtdataacesso"); 
        

        if($txtstate == 'add')
        {
            $insert = array(
                'curso_id' => $txtcursoid, 'diciplina_id' => $txtdiciplinaid, 'operador_id' => $txtoperadorid, 'data_acesso' => date("Y-m-d H:i:s", strtotime($txtdataacesso)),         
            );
            insertData($this->table_db, $insert);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
        else
        {            
            $update = array(
                'curso_id' => $txtcursoid, 'diciplina_id' => $txtdiciplinaid, 'operador_id' => $txtoperadorid, 'data_acesso' => date("Y-m-d H:i:s", strtotime($txtdataacesso)),              
            );
            updateData($this->table_db, $update, "row_id = '".$txtrowid."'", $txtrowid);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
    }

    public function delete(){
        $txtrowid = $this->input->post("txtrowid");
        
        deleteData($this->table_db, "row_id = '".$txtrowid."'");
        $data['msg'] = "Process successful";
        $data['type'] = 1;
        echo json_encode($data);
    }
}	