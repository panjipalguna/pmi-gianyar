<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Assicurazione extends CI_Controller {
    function __construct() {
        parent::__construct();
        islogin();
    }

    public $table_db = "damiano";
    public $controller = "assicurazione";
    
    public function index() {
        $lbl_controller = str_replace("_", " ", $this->controller);
        $page_url = site_url($this->controller);
		$data = array(
            'controller' => $this->controller,
            'lbl_controller' => $lbl_controller,
            'page_url' => $page_url,
            
        );
        $this->layout->display('page/assicurazione_vw', $data);
    }

    public function gridview() {
        $where = $this->input->post("where");        
        $database = "default";
        $column = array('row_id', 'nome', 'quietanza', 'documento');
        $table = "damiano";
        $list = $this->datatables_mdl->get_datatables($database, $table, $column, $where);
        $data = array();
        $no = $_POST['start'];
        foreach ($list as $line) 
        {
            $no++;          
            $row = array();
            $link = '
                <div class="btn-group">
                  <a href="javascript:void(0)" data-toggle="dropdown" style="color:#111"><i class="fa fa-folder-open"></i></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="javascript:void(0)" style="color:#111" data-toggle="modal" data-target="#modalForm" onclick="generateModalView(\''.$line->row_id.'\')">View</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="generateModalForm(\'edit\', \''.$line->row_id.'\')">Update</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="doDelete(\''.$line->row_id.'\')">Delete</a></li>
                  </ul>
                </div>
            ';
            $row[] = '<div style="text-align:center">'.$link.'</div>';
            $row[] = '<div style="text-align:left">'.$line->nome.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->quietanza.'</div>'; 

            $data[] = $row;
        }
 
        $output = array(
            "draw" => $_POST['draw'],
            "recordsTotal" => $this->datatables_mdl->count_all($database, $table, $column),
            "recordsFiltered" => $this->datatables_mdl->count_filtered($database, $table, $column, $where),
            "data" => $data,
        );
        
        echo json_encode($output);
    }

    public function form() {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtnome = ''; 
$txtquietanza = ''; 
$txtdocumento = ''; 


        if($txtstate == "edit") {
            $qry = "select * from damiano where row_id = '".$txtrowid."'";
            $rs = getByQuery($qry);
            if($rs != "") {
                $txtnome = $rs[0]->nome; 
$txtquietanza = $rs[0]->quietanza; 
$txtdocumento = $rs[0]->documento; 
$txtdocumento_hdn = $rs[0]->documento; 

            }
        }

        

        $table = '
            <div class="row">
                <form method="POST" class="formInput" enctype="multipart/form-data">
                    <input type="hidden" id="txtrowid" name="txtrowid" class="form-control" value="'.$txtrowid.'" style="background:#fff" readonly />
                    <input type="hidden" id="txtstate" name="txtstate" class="form-control" value="'.$txtstate.'" />
                    
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Nome:</label>
                        <textarea id="txtnome" name="txtnome" rows="4" class="form-control">'.$txtnome.'</textarea>
                      </div>
                    </div>
                  
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Prezzo:</label>
                        <input type="text" id="txtquietanza" name="txtquietanza" class="form-control" value="'.$txtquietanza.'" />
                      </div>
                    </div>
                  
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Aggiundi Documento: <span style="font-size:10px">(leave it blank if you do not want to update)</span></label>
                        <input type="file" id="txtdocumento" name="txtdocumento" class="form-control" />
                        <input type="hidden" id="txtdocumento_hdn" name="txtdocumento_hdn" class="form-control" value="'.$txtdocumento.'" />
                      </div>
                    </div>
                  
                </form>
            </div>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function view() {
        $txtrowid = $this->input->post("txtrowid");
        $txtnome = ''; 
$txtquietanza = ''; 
$txtdocumento = ''; 

        
        $qry = "select * from damiano where row_id = '".$txtrowid."'";
        $rs = getByQuery($qry);
        if($rs != "") {
                $txtnome = $rs[0]->nome; 
$txtquietanza = $rs[0]->quietanza; 
$txtdocumento = $rs[0]->documento; 
$txtdocumento_hdn = $rs[0]->documento; 

        }        

        $table = '
            <table id="tablelist" class="table table-striped">
              <tbody>
                
                    <tr>
                        <td class="view-title" style="width: 30%">Nome:</td>
                        <td class="view-txt">'.$txtnome.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Prezzo:</td>
                        <td class="view-txt">'.$txtquietanza.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Aggiundi Documento:</td>
                        <td class="view-txt"><img style="width:100px" src="'.base_url("uploaded/".$txtdocumento).'" /></td>
                    </tr>
                  
              </tbody>
            </table>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function save()
    {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtnome = $this->input->post("txtnome"); $txtquietanza = $this->input->post("txtquietanza"); $txtdocumento = $this->input->post("txtdocumento"); $txtdocumento_hdn = $this->input->post("txtdocumento_hdn"); 
        
                $txtdocumento =  $txtdocumento_hdn;
                if($_FILES['txtdocumento']['name'] != '')
                {
                     $ext = pathinfo($_FILES['txtdocumento']['name'], PATHINFO_EXTENSION);
                     $file_name = date('YmdHis').'.'.$ext;
                     $config = array(
                        'upload_path' => 'uploaded',
                        'allowed_types' => 'gif|jpg|png|jpeg|pdf|docx|xls|xlsx|doc|txt',
                        'overwrite' => TRUE,
                        'file_name' =>  $file_name
                    );
                     $this->upload->initialize( $config);
                    if( $this->upload->do_upload('txtdocumento'))
                    {
                         $data = $this->upload->data();
                        $txtdocumento =  $file_name;
                        if($txtdocumento_hdn != '')
                          unlink('uploaded/'.$txtdocumento_hdn);
                    }
                    else
                    {
                        $data['msg'] = $this->upload->display_errors();
                        $data['type'] = 0;
                        echo json_encode($data);
                        exit();
                    }
                }
              

        if($txtstate == 'add')
        {
            $insert = array(
                'nome' => $txtnome, 'quietanza' => $txtquietanza, 'documento' => $txtdocumento,         
            );
            insertData($this->table_db, $insert);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
        else
        {            
            $update = array(
                'nome' => $txtnome, 'quietanza' => $txtquietanza, 'documento' => $txtdocumento,              
            );
            updateData($this->table_db, $update, "row_id = '".$txtrowid."'", $txtrowid);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
    }

    public function delete(){
        $txtrowid = $this->input->post("txtrowid");
        
                $rs_damiano = getByQuery("select * from damiano where row_id = ".$txtrowid);
                if($rs_damiano != "") {
                    foreach($rs_damiano as $row) {
                        unlink('uploaded/'.$row->documento); 

                    }
                }
              
        deleteData($this->table_db, "row_id = '".$txtrowid."'");
        $data['msg'] = "Process successful";
        $data['type'] = 1;
        echo json_encode($data);
    }
}	