<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Artist extends CI_Controller {
    function __construct() {
        parent::__construct();
        islogin();
    }

    public $table_db = "artist";
    public $controller = "artist";
    
    public function index() {
        $lbl_controller = str_replace("_", " ", $this->controller);
        $page_url = site_url($this->controller);
		$data = array(
            'controller' => $this->controller,
            'lbl_controller' => $lbl_controller,
            'page_url' => $page_url,
            
        );
        $this->layout->display('page/artist_vw', $data);
    }

    public function gridview() {
        $where = $this->input->post("where");        
        $database = "default";
        $column = array('row_id', 'Artist Name ', 'artist image');
        $table = "artist";
        $list = $this->datatables_mdl->get_datatables($database, $table, $column, $where);
        $data = array();
        $no = $_POST['start'];
        foreach ($list as $line) 
        {
            $no++;          
            $row = array();
            $link = '
                <div class="btn-group">
                  <a href="javascript:void(0)" data-toggle="dropdown" style="color:#111"><i class="fa fa-folder-open"></i></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="javascript:void(0)" style="color:#111" data-toggle="modal" data-target="#modalForm" onclick="generateModalView(\''.$line->row_id.'\')">View</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="generateModalForm(\'edit\', \''.$line->row_id.'\')">Update</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="doDelete(\''.$line->row_id.'\')">Delete</a></li>
                  </ul>
                </div>
            ';
            $row[] = '<div style="text-align:center">'.$link.'</div>';
            $row[] = '<div style="text-align:left">'.$line->Artist Name .'</div>'; 

            $data[] = $row;
        }
 
        $output = array(
            "draw" => $_POST['draw'],
            "recordsTotal" => $this->datatables_mdl->count_all($database, $table, $column),
            "recordsFiltered" => $this->datatables_mdl->count_filtered($database, $table, $column, $where),
            "data" => $data,
        );
        
        echo json_encode($output);
    }

    public function form() {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtArtist Name  = ''; 
$txtartist image = ''; 


        if($txtstate == "edit") {
            $qry = "select * from artist where row_id = '".$txtrowid."'";
            $rs = getByQuery($qry);
            if($rs != "") {
                $txtArtist Name  = $rs[0]->Artist Name ; 
$txtartist image = $rs[0]->artist image; 
$txtartist image_hdn = $rs[0]->artist image; 

            }
        }

        

        $table = '
            <div class="row">
                <form method="POST" class="formInput" enctype="multipart/form-data">
                    <input type="hidden" id="txtrowid" name="txtrowid" class="form-control" value="'.$txtrowid.'" style="background:#fff" readonly />
                    <input type="hidden" id="txtstate" name="txtstate" class="form-control" value="'.$txtstate.'" />
                    
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Artist Name  Here: <span style="color: #ff0000">*</span></label>
                        <textarea id="txtArtist Name " name="txtArtist Name " rows="4" class="form-control">'.$txtArtist Name .'</textarea>
                      </div>
                    </div>
                  
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Artist Imaage Here: <span style="color: #ff0000">*</span> <span style="font-size:10px">(leave it blank if you do not want to update)</span></label>
                        <input type="file" id="txtartist image" name="txtartist image" class="form-control" />
                        <input type="hidden" id="txtartist image_hdn" name="txtartist image_hdn" class="form-control" value="'.$txtartist image.'" />
                      </div>
                    </div>
                  
                </form>
            </div>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function view() {
        $txtrowid = $this->input->post("txtrowid");
        $txtArtist Name  = ''; 
$txtartist image = ''; 

        
        $qry = "select * from artist where row_id = '".$txtrowid."'";
        $rs = getByQuery($qry);
        if($rs != "") {
                $txtArtist Name  = $rs[0]->Artist Name ; 
$txtartist image = $rs[0]->artist image; 
$txtartist image_hdn = $rs[0]->artist image; 

        }        

        $table = '
            <table id="tablelist" class="table table-striped">
              <tbody>
                
                    <tr>
                        <td class="view-title" style="width: 30%">Artist Name  Here:</td>
                        <td class="view-txt">'.$txtArtist Name .'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Artist Imaage Here:</td>
                        <td class="view-txt"><img style="width:100px" src="'.base_url("uploaded/".$txtartist image).'" /></td>
                    </tr>
                  
              </tbody>
            </table>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function save()
    {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtArtist Name  = $this->input->post("txtArtist Name "); $txtartist image = $this->input->post("txtartist image"); $txtartist image_hdn = $this->input->post("txtartist image_hdn"); 
        
                $txtartist image =  $txtartist image_hdn;
                if($_FILES['txtartist image']['name'] != '')
                {
                     $ext = pathinfo($_FILES['txtartist image']['name'], PATHINFO_EXTENSION);
                     $file_name = date('YmdHis').'.'.$ext;
                     $config = array(
                        'upload_path' => 'uploaded',
                        'allowed_types' => 'gif|jpg|png|jpeg|pdf|docx|xls|xlsx|doc|txt',
                        'overwrite' => TRUE,
                        'file_name' =>  $file_name
                    );
                     $this->upload->initialize( $config);
                    if( $this->upload->do_upload('txtartist image'))
                    {
                         $data = $this->upload->data();
                        $txtartist image =  $file_name;
                        if($txtartist image_hdn != '')
                          unlink('uploaded/'.$txtartist image_hdn);
                    }
                    else
                    {
                        $data['msg'] = $this->upload->display_errors();
                        $data['type'] = 0;
                        echo json_encode($data);
                        exit();
                    }
                }
              

        if($txtstate == 'add')
        {
            $insert = array(
                'Artist Name ' => $txtArtist Name , 'artist image' => $txtartist image,         
            );
            insertData($this->table_db, $insert);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
        else
        {            
            $update = array(
                'Artist Name ' => $txtArtist Name , 'artist image' => $txtartist image,              
            );
            updateData($this->table_db, $update, "row_id = '".$txtrowid."'", $txtrowid);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
    }

    public function delete(){
        $txtrowid = $this->input->post("txtrowid");
        
                $rs_artist = getByQuery("select * from artist where row_id = ".$txtrowid);
                if($rs_artist != "") {
                    foreach($rs_artist as $row) {
                        unlink('uploaded/'.$row->artist image); 

                    }
                }
              
        deleteData($this->table_db, "row_id = '".$txtrowid."'");
        $data['msg'] = "Process successful";
        $data['type'] = 1;
        echo json_encode($data);
    }
}	