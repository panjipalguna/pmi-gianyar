<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Pessoa extends CI_Controller {
    function __construct() {
        parent::__construct();
        islogin();
    }

    public $table_db = "pessoatable";
    public $controller = "pessoa";
    
    public function index() {
        $lbl_controller = str_replace("_", " ", $this->controller);
        $page_url = site_url($this->controller);
		$data = array(
            'controller' => $this->controller,
            'lbl_controller' => $lbl_controller,
            'page_url' => $page_url,
            'rs_pessoatable' => getByQuery('select * from pessoatable'),
                
        );
        $this->layout->display('page/pessoa_vw', $data);
    }

    public function gridview() {
        $where = $this->input->post("where");        
        $database = "default";
        $column = array('row_id', 'id', 'nome', 'endereco', 'data');
        $table = "vw_pessoatablelist";
        $list = $this->datatables_mdl->get_datatables($database, $table, $column, $where);
        $data = array();
        $no = $_POST['start'];
        foreach ($list as $line) 
        {
            $no++;          
            $row = array();
            $link = '
                <div class="btn-group">
                  <a href="javascript:void(0)" data-toggle="dropdown" style="color:#111"><i class="fa fa-folder-open"></i></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="javascript:void(0)" style="color:#111" data-toggle="modal" data-target="#modalForm" onclick="generateModalView(\''.$line->row_id.'\')">View</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="generateModalForm(\'edit\', \''.$line->row_id.'\')">Update</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="doDelete(\''.$line->row_id.'\')">Delete</a></li>
                  </ul>
                </div>
            ';
            $row[] = '<div style="text-align:center">'.$link.'</div>';
            $row[] = '<div style="text-align:center">'.$line->pessoatable_NOME.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->nome.'</div>'; 
$row[] = '<div style="text-align:left">'.$line->endereco.'</div>'; 
$row[] = '<div style="text-align:center">'.date("m/d/Y", strtotime($line->data)).'</div>'; 

            $data[] = $row;
        }
 
        $output = array(
            "draw" => $_POST['draw'],
            "recordsTotal" => $this->datatables_mdl->count_all($database, $table, $column),
            "recordsFiltered" => $this->datatables_mdl->count_filtered($database, $table, $column, $where),
            "data" => $data,
        );
        
        echo json_encode($output);
    }

    public function form() {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtid = ''; 
$pessoatable_list = "";$txtpessoatableNOME = ''; 
$txtnome = ''; 
$txtendereco = ''; 
$txtdata = ''; 


        if($txtstate == "edit") {
            $qry = "select * from vw_pessoatablelist where row_id = '".$txtrowid."'";
            $rs = getByQuery($qry);
            if($rs != "") {
                $txtid = $rs[0]->id; 
$txtpessoatableNOME = $rs[0]->pessoatable_NOME; 
$txtnome = $rs[0]->nome; 
$txtendereco = $rs[0]->endereco; 
$txtdata = date('Y-m-d', strtotime($rs[0]->data)); 

            }
        }

        
                  $rs_pessoatable = getByQuery("select * from pessoatable");
                  if($rs_pessoatable != "") {
                      foreach($rs_pessoatable as $row_pessoatable) {
                          $selected = ($row_pessoatable->teste_id == $txtid) ? "selected" : "";
                          $pessoatable_list .= "<option value='".$row_pessoatable->teste_id."' ".$selected." >".$row_pessoatable->NOME."</option>";
                      }
                  }
                

        $table = '
            <div class="row">
                <form method="POST" class="formInput" enctype="multipart/form-data">
                    <input type="hidden" id="txtrowid" name="txtrowid" class="form-control" value="'.$txtrowid.'" style="background:#fff" readonly />
                    <input type="hidden" id="txtstate" name="txtstate" class="form-control" value="'.$txtstate.'" />
                    
                  <div class="col-md-12">
                    <div class="form-group">
                      <label>Departamento: <span style="color: #ff0000">*</span></label>
                      <select id="txtid" name="txtid" class="form-control select2"  required>
                        <option value=""></option>
                        '.$pessoatable_list.'
                      </select>
                    </div>
                  </div>
                
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Tb Nome: <span style="color: #ff0000">*</span></label>
                        <input type="text" id="txtnome" name="txtnome" class="form-control" value="'.$txtnome.'" />
                      </div>
                    </div>
                  
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Tb Endereco:</label>
                        <textarea id="txtendereco" name="txtendereco" rows="4" class="form-control">'.$txtendereco.'</textarea>
                      </div>
                    </div>
                  
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Tb Data:</label>
                        <input type="date" id="txtdata" name="txtdata" class="form-control" value="'.$txtdata.'" />
                      </div>
                    </div>
                  
                </form>
            </div>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function view() {
        $txtrowid = $this->input->post("txtrowid");
        $txtid = ''; 
$pessoatable_list = "";$txtpessoatableNOME = ''; 
$txtnome = ''; 
$txtendereco = ''; 
$txtdata = ''; 

        
        $qry = "select * from vw_pessoatablelist where row_id = '".$txtrowid."'";
        $rs = getByQuery($qry);
        if($rs != "") {
                $txtid = $rs[0]->id; 
$txtpessoatableNOME = $rs[0]->pessoatable_NOME; 
$txtnome = $rs[0]->nome; 
$txtendereco = $rs[0]->endereco; 
$txtdata = date('Y-m-d', strtotime($rs[0]->data)); 

        }        

        $table = '
            <table id="tablelist" class="table table-striped">
              <tbody>
                
                  <tr>
                      <td class="view-title" style="width: 30%">Departamento:</td>
                      <td class="view-txt">'.$txtpessoatableNOME.'</td>
                  </tr>
                
                    <tr>
                        <td class="view-title" style="width: 30%">Tb Nome:</td>
                        <td class="view-txt">'.$txtnome.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Tb Endereco:</td>
                        <td class="view-txt">'.$txtendereco.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Tb Data:</td>
                        <td class="view-txt">'.$txtdata.'</td>
                    </tr>
                  
              </tbody>
            </table>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function save()
    {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtid = $this->input->post("txtid"); $txtnome = $this->input->post("txtnome"); $txtendereco = $this->input->post("txtendereco"); $txtdata = $this->input->post("txtdata"); 
        

        if($txtstate == 'add')
        {
            $insert = array(
                'id' => $txtid, 'nome' => $txtnome, 'endereco' => $txtendereco, 'data' => date("Y-m-d H:i:s", strtotime($txtdata)),         
            );
            insertData($this->table_db, $insert);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
        else
        {            
            $update = array(
                'id' => $txtid, 'nome' => $txtnome, 'endereco' => $txtendereco, 'data' => date("Y-m-d H:i:s", strtotime($txtdata)),              
            );
            updateData($this->table_db, $update, "row_id = '".$txtrowid."'", $txtrowid);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
    }

    public function delete(){
        $txtrowid = $this->input->post("txtrowid");
        
        deleteData($this->table_db, "row_id = '".$txtrowid."'");
        $data['msg'] = "Process successful";
        $data['type'] = 1;
        echo json_encode($data);
    }
}	