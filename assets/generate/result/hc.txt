<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Hc extends CI_Controller {
    function __construct() {
        parent::__construct();
        islogin();
    }

    public $table_db = "clocks";
    public $controller = "hc";
    
    public function index() {
        $lbl_controller = str_replace("_", " ", $this->controller);
        $page_url = site_url($this->controller);
		$data = array(
            'controller' => $this->controller,
            'lbl_controller' => $lbl_controller,
            'page_url' => $page_url,
            
        );
        $this->layout->display('page/hc_vw', $data);
    }

    public function gridview() {
        $where = $this->input->post("where");        
        $database = "default";
        $column = array('row_id', 'holiday_name', 'clock_id', 'clock_date', 'img_id', 'ClockDone', 'id_num');
        $table = "clocks";
        $list = $this->datatables_mdl->get_datatables($database, $table, $column, $where);
        $data = array();
        $no = $_POST['start'];
        foreach ($list as $line) 
        {
            $no++;          
            $row = array();
            $link = '
                <div class="btn-group">
                  <a href="javascript:void(0)" data-toggle="dropdown" style="color:#111"><i class="fa fa-folder-open"></i></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="javascript:void(0)" style="color:#111" data-toggle="modal" data-target="#modalForm" onclick="generateModalView(\''.$line->row_id.'\')">View</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="generateModalForm(\'edit\', \''.$line->row_id.'\')">Update</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="doDelete(\''.$line->row_id.'\')">Delete</a></li>
                  </ul>
                </div>
            ';
            $row[] = '<div style="text-align:center">'.$link.'</div>';
            $row[] = '<div style="text-align:center">'.$line->holiday_name.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->clock_id.'</div>'; 
$row[] = '<div style="text-align:center">'.date("m/d/Y", strtotime($line->clock_date)).'</div>'; 
$row[] = '<div style="text-align:center">'.$line->img_id.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->ClockDone.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->id_num.'</div>'; 

            $data[] = $row;
        }
 
        $output = array(
            "draw" => $_POST['draw'],
            "recordsTotal" => $this->datatables_mdl->count_all($database, $table, $column),
            "recordsFiltered" => $this->datatables_mdl->count_filtered($database, $table, $column, $where),
            "data" => $data,
        );
        
        echo json_encode($output);
    }

    public function form() {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtholidayname = ''; 
$txtclockid = ''; 
$txtclockdate = ''; 
$txtimgid = ''; 
$txtClockDone = ''; 
$txtidnum = ''; 


        if($txtstate == "edit") {
            $qry = "select * from clocks where row_id = '".$txtrowid."'";
            $rs = getByQuery($qry);
            if($rs != "") {
                $txtholidayname = $rs[0]->holiday_name; 
$txtclockid = $rs[0]->clock_id; 
$txtclockdate = date('Y-m-d', strtotime($rs[0]->clock_date)); 
$txtimgid = $rs[0]->img_id; 
$txtClockDone = $rs[0]->ClockDone; 
$txtidnum = $rs[0]->id_num; 

            }
        }

        

        $table = '
            <div class="row">
                <form method="POST" class="formInput" enctype="multipart/form-data">
                    <input type="hidden" id="txtrowid" name="txtrowid" class="form-control" value="'.$txtrowid.'" style="background:#fff" readonly />
                    <input type="hidden" id="txtstate" name="txtstate" class="form-control" value="'.$txtstate.'" />
                    
                    <div class="col-md-6">
                      <div class="form-group">
                        <label>Holiday Name:</label>
                        <input type="text" id="txtholidayname" name="txtholidayname" class="form-control" value="'.$txtholidayname.'" />
                      </div>
                    </div>
                  
                    <div class="col-md-6">
                      <div class="form-group">
                        <label>Clock ID (URL):</label>
                        <input type="text" id="txtclockid" name="txtclockid" class="form-control" value="'.$txtclockid.'" />
                      </div>
                    </div>
                  
                    <div class="col-md-6">
                      <div class="form-group">
                        <label>Clock Date:</label>
                        <input type="date" id="txtclockdate" name="txtclockdate" class="form-control" value="'.$txtclockdate.'" />
                      </div>
                    </div>
                  
                    <div class="col-md-6">
                      <div class="form-group">
                        <label>Img Id:</label>
                        <input type="text" id="txtimgid" name="txtimgid" class="form-control" value="'.$txtimgid.'" />
                      </div>
                    </div>
                  
                    <div class="col-md-6">
                      <div class="form-group">
                        <label>ClockDone:</label>
                        <input type="text" id="txtClockDone" name="txtClockDone" class="form-control" value="'.$txtClockDone.'" />
                      </div>
                    </div>
                  
                    <div class="col-md-6">
                      <div class="form-group">
                        <label>Id Num:</label>
                        <input type="number" step="1" " id="txtidnum" name="txtidnum" class="form-control" value="'.$txtidnum.'" />
                      </div>
                    </div>
                  
                </form>
            </div>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function view() {
        $txtrowid = $this->input->post("txtrowid");
        $txtholidayname = ''; 
$txtclockid = ''; 
$txtclockdate = ''; 
$txtimgid = ''; 
$txtClockDone = ''; 
$txtidnum = ''; 

        
        $qry = "select * from clocks where row_id = '".$txtrowid."'";
        $rs = getByQuery($qry);
        if($rs != "") {
                $txtholidayname = $rs[0]->holiday_name; 
$txtclockid = $rs[0]->clock_id; 
$txtclockdate = date('Y-m-d', strtotime($rs[0]->clock_date)); 
$txtimgid = $rs[0]->img_id; 
$txtClockDone = $rs[0]->ClockDone; 
$txtidnum = $rs[0]->id_num; 

        }        

        $table = '
            <table id="tablelist" class="table table-striped">
              <tbody>
                
                    <tr>
                        <td class="view-title" style="width: 30%">Holiday Name:</td>
                        <td class="view-txt">'.$txtholidayname.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Clock ID (URL):</td>
                        <td class="view-txt">'.$txtclockid.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Clock Date:</td>
                        <td class="view-txt">'.$txtclockdate.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Img Id:</td>
                        <td class="view-txt">'.$txtimgid.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">ClockDone:</td>
                        <td class="view-txt">'.$txtClockDone.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Id Num:</td>
                        <td class="view-txt">'.$txtidnum.'</td>
                    </tr>
                  
              </tbody>
            </table>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function save()
    {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtholidayname = $this->input->post("txtholidayname"); $txtclockid = $this->input->post("txtclockid"); $txtclockdate = $this->input->post("txtclockdate"); $txtimgid = $this->input->post("txtimgid"); $txtClockDone = $this->input->post("txtClockDone"); $txtidnum = $this->input->post("txtidnum"); 
        

        if($txtstate == 'add')
        {
            $insert = array(
                'holiday_name' => $txtholidayname, 'clock_id' => $txtclockid, 'clock_date' => date("Y-m-d H:i:s", strtotime($txtclockdate)), 'img_id' => $txtimgid, 'ClockDone' => $txtClockDone, 'id_num' => $txtidnum,         
            );
            insertData($this->table_db, $insert);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
        else
        {            
            $update = array(
                'holiday_name' => $txtholidayname, 'clock_id' => $txtclockid, 'clock_date' => date("Y-m-d H:i:s", strtotime($txtclockdate)), 'img_id' => $txtimgid, 'ClockDone' => $txtClockDone, 'id_num' => $txtidnum,              
            );
            updateData($this->table_db, $update, "row_id = '".$txtrowid."'", $txtrowid);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
    }

    public function delete(){
        $txtrowid = $this->input->post("txtrowid");
        
        deleteData($this->table_db, "row_id = '".$txtrowid."'");
        $data['msg'] = "Process successful";
        $data['type'] = 1;
        echo json_encode($data);
    }
}	