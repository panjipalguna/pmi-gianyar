<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Sd extends CI_Controller {
    function __construct() {
        parent::__construct();
    }

    public $table_db = "fdsfds";
    public $controller = "sd";
    
    public function index() {
        $lbl_controller = str_replace("_", " ", $this->controller);
        $page_url = site_url($this->controller);
		$data = array(
            'controller' => $this->controller,
            'lbl_controller' => $lbl_controller,
            'page_url' => $page_url,
            
        );
        $this->layout->display('page/sd_vw', $data);
    }

    public function gridview() {
        $where = $this->input->post("where");        
        $database = "default";
        $column = array('row_id', 'sad', 'fds', 'fds');
        $table = "fdsfds";
        $list = $this->datatables_mdl->get_datatables($database, $table, $column, $where);
        $data = array();
        $no = $_POST['start'];
        foreach ($list as $line) 
        {
            $no++;          
            $row = array();
            $link = '
                <div class="btn-group">
                  <a href="javascript:void(0)" data-toggle="dropdown" style="color:#111"><i class="fa fa-folder-open"></i></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="javascript:void(0)" style="color:#111" data-toggle="modal" data-target="#modalForm" onclick="generateModalView(\''.$line->row_id.'\')">View</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="generateModalForm(\'edit\', \''.$line->row_id.'\')">Update</a></li>
                    <li><a href="javascript:void(0)" style="color:#111" onclick="doDelete(\''.$line->row_id.'\')">Delete</a></li>
                  </ul>
                </div>
            ';
            $row[] = '<div style="text-align:center">'.$link.'</div>';
            $row[] = '<div style="text-align:center">'.$line->sad.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->fds.'</div>'; 
$row[] = '<div style="text-align:center">'.$line->fds.'</div>'; 

            $data[] = $row;
        }
 
        $output = array(
            "draw" => $_POST['draw'],
            "recordsTotal" => $this->datatables_mdl->count_all($database, $table, $column),
            "recordsFiltered" => $this->datatables_mdl->count_filtered($database, $table, $column, $where),
            "data" => $data,
        );
        
        echo json_encode($output);
    }

    public function form() {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtsad = ''; 
$txtfds = ''; 
$txtfds = ''; 


        if($txtstate == "edit") {
            $qry = "select * from fdsfds where row_id = '".$txtrowid."'";
            $rs = getByQuery($qry);
            if($rs != "") {
                $txtsad = $rs[0]->sad; 
$txtfds = $rs[0]->fds; 
$txtfds = $rs[0]->fds; 

            }
        }

        

        $table = '
            <div class="row">
                <form method="POST" class="formInput" enctype="multipart/form-data">
                    <input type="hidden" id="txtrowid" name="txtrowid" class="form-control" value="'.$txtrowid.'" style="background:#fff" readonly />
                    <input type="hidden" id="txtstate" name="txtstate" class="form-control" value="'.$txtstate.'" />
                    
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Gdsa:</label>
                        <input type="text" id="txtsad" name="txtsad" class="form-control" value="'.$txtsad.'" />
                      </div>
                    </div>
                  
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Fdsa:</label>
                        <input type="text" id="txtfds" name="txtfds" class="form-control" value="'.$txtfds.'" />
                      </div>
                    </div>
                  
                    <div class="col-md-12">
                      <div class="form-group">
                        <label>Dsa:</label>
                        <input type="text" id="txtfds" name="txtfds" class="form-control" value="'.$txtfds.'" />
                      </div>
                    </div>
                  
                </form>
            </div>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function view() {
        $txtrowid = $this->input->post("txtrowid");
        $txtsad = ''; 
$txtfds = ''; 
$txtfds = ''; 

        
        $qry = "select * from fdsfds where row_id = '".$txtrowid."'";
        $rs = getByQuery($qry);
        if($rs != "") {
                $txtsad = $rs[0]->sad; 
$txtfds = $rs[0]->fds; 
$txtfds = $rs[0]->fds; 

        }        

        $table = '
            <table id="tablelist" class="table table-striped">
              <tbody>
                
                    <tr>
                        <td class="view-title" style="width: 30%">Gdsa:</td>
                        <td class="view-txt">'.$txtsad.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Fdsa:</td>
                        <td class="view-txt">'.$txtfds.'</td>
                    </tr>
                  
                    <tr>
                        <td class="view-title" style="width: 30%">Dsa:</td>
                        <td class="view-txt">'.$txtfds.'</td>
                    </tr>
                  
              </tbody>
            </table>
        ';

        $data['table'] = $table;
        echo json_encode($data);
    }

    public function save()
    {
        $txtstate = $this->input->post("txtstate");
        $txtrowid = $this->input->post("txtrowid");
        $txtsad = $this->input->post("txtsad"); $txtfds = $this->input->post("txtfds"); $txtfds = $this->input->post("txtfds"); 
        

        if($txtstate == 'add')
        {
            $insert = array(
                'sad' => $txtsad, 'fds' => $txtfds, 'fds' => $txtfds,         
            );
            insertData($this->table_db, $insert);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
        else
        {            
            $update = array(
                'sad' => $txtsad, 'fds' => $txtfds, 'fds' => $txtfds,              
            );
            updateData($this->table_db, $update, "row_id = '".$txtrowid."'", $txtrowid);
            $data['msg'] = "Process successful";
            $data['type'] = 1;
            echo json_encode($data);
        }
    }

    public function delete(){
        $txtrowid = $this->input->post("txtrowid");
        
        deleteData($this->table_db, "row_id = '".$txtrowid."'");
        $data['msg'] = "Process successful";
        $data['type'] = 1;
        echo json_encode($data);
    }
}	